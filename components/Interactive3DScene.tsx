'use client'

// Temporarily disabled to fix React conflicts
// Will be re-enabled with proper isolation
export default function Interactive3DScene() {
  return <div className="w-full h-full bg-dark-bg" />
}
