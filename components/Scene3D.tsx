'use client'

export default function Scene3D() {
  // Temporarily disabled to fix React errors
  // Will be re-enabled once React version conflicts are resolved
  return <div className="w-full h-full bg-dark-bg" />
}
